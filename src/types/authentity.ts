export type PayloadItem = {
    userId: number;
    id: number;
    title: string;
    body: string;
};
export type SuccessResponsePayload = {
    userId: number;
    id: number;
    title: string;
    body: string;
};
